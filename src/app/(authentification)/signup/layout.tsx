import "@/styles/globals.css";

export const metadata = {
  title: "Signup to Educraft Server",
};

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode; 
}) {
  return children ;
}